import React from 'react';

export default function Header({ title }) {
  return (
    <header>
      <h1>{title}</h1>
    </header>
  );
}

//Children = Utilizado para acessar o conteudo criado dentro do componente/propriedade.
